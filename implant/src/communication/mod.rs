// C2 communication channels — T1071
//
// https  : primary beacon over HTTPS port 443 with random jitter (reqwest + rustls)
// dns    : fallback DNS TXT tunneling port 53 (if HTTP blocked — noisy, NTA-detectable)
